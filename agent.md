## Coding rules

- Do not overwrite raw data.
- Save intermediate cleaned data to `data/temp/`.
- Save figures to `output/figures/`.
- Save regression tables to `output/tables/`.
- I might give you some undeveloped instruction. If the command is not clear enough, always ask me the details then conduct it.
- **Do not add unnecessary features or fallback checks.** Always start from simple and clear code, then improve it under my command.